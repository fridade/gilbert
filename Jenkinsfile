pipeline {
    agent any

    stages {
        stage('create directory') {
            steps {
                sh 'mkdir gilbert'
            }
        }
        stage('create file') {
            steps {
                sh 'touch fritz'
            }
        }
    }
}
