pipeline {
    agent any

    stages {
        stage('create directory') {
            steps {
                sh 'mkdir gilbert'
            }
        }
        stage('create directory') {
            steps {
                sh 'touch fritz'
            }
        }
    }
}
